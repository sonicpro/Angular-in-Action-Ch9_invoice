import { HoursDirective } from './hours.directive';

describe('HoursDirective', () => {
  it('should create an instance', () => {
    const directive = new HoursDirective();
    expect(directive).toBeTruthy();
  });
});
